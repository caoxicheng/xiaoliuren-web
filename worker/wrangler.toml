name = "xiaoliuren-ai"
main = "index.js"
compatibility_date = "2024-01-01"

# 本地开发测试用
# 开发时运行: wrangler dev --local
# 部署时运行: wrangler deploy

[vars]
# 可以在这里放默认值（不推荐放 secrets）
ENVIRONMENT = "production"
